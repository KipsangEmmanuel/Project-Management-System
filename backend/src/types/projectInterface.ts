export interface Project {
  project_id: string;
  project_name: string;
  project_description: string;
  dueDate: string;
}
// export interface project extends updatProject {
//   isdeleted: boolean;
// }
